spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/example?useSSL=false
    username: root
    password: example

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

  sql:
    init:
      mode: always